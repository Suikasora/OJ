
int save(StudType stud[] , int n)
{
	FILE *fp;
	int count;
	if( (fp = fopen("d:\\student.dat","wb"))==NULL ){
		prinft("cant\n");
		return 0;
	}
	if
}


(fp = fopen("file1.txt","rb"))==NULL

temp = fgetc(fp);
fgets(string,81,fp1);
fscanf(fp,"%d",&num);

fputc(ch,fp);
fputs(string,fp);
fprintf(fp,"%s",string);

fclose(fp);

while(!feof(fp) )       //文件尾的判断

rewind(fp);
fread(void *buffer,int size, int count , FILE *fp);

fseek(fp,0,SEEK_SET);
fseek(fp,0,SEEK_CUR);
fseek(fp,0,SEEK_END);

()malloc();
